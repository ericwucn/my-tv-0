name: build

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Run build with Gradle wrapper
        run: |
          set -e
          ./gradlew clean 2>&1 | tee gradle_clean.log
          ./gradlew assembleRelease 2>&1 | tee gradle_build.log
          echo "=== GRADLE BUILD RESULT ==="
          echo "Build exit code: $?"
          tail -100 gradle_build.log

      - name: Debug on failure
        if: failure()
        run: |
          echo "=== last 100 lines of gradle_build.log ==="
          tail -100 gradle_build.log || true
          echo "=== gradle build directory ==="
          ls -la app/build/outputs/apk/release/ 2>&1 || echo "No release dir"
          echo "=== project files ==="
          ls -la app/src/main/java/com/lizongying/mytv0/ | head -20

      - name: Sign app APK
        id: sign_app
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: app/build/outputs/apk/release
          alias: ${{ secrets.ALIAS }}
          signingKeyBase64: ${{ secrets.KEYSTORE }}
          keyStorePassword: ${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.ALIAS_PASSWORD }}
        env:
          # override default build-tools version (29.0.3) -- optional
          BUILD_TOOLS_VERSION: "34.0.0"

      - name: Rename APK with version
        id: rename_apk
        run: |
          # 获取版本号（tag 名称，如 v1.5.1.8）
          VERSION="${{ github.ref_name }}"
          
          # 构建新文件名: my-tv-0-v1.5.1.8.apk
          NEW_NAME="my-tv-0-${VERSION}.apk"
          
          # 重命名文件
          SIGNED_FILE="${{ steps.sign_app.outputs.signedReleaseFile }}"
          mv "$SIGNED_FILE" "$(dirname "$SIGNED_FILE")/$NEW_NAME"
          
          # 输出新的文件路径
          echo "signedReleaseFile=$(dirname "$SIGNED_FILE")/$NEW_NAME" >> $GITHUB_OUTPUT
          echo "apkName=$NEW_NAME" >> $GITHUB_OUTPUT
          echo "Renamed APK to: $NEW_NAME"

      - name: Get History
        id: get_history
        run: |
          chmod +x history.sh
          output=$(./history.sh)
          echo "$output" > history.md

      - name: Delete existing release if exists
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete "${{ github.ref_name }}" --yes || true

      - name: Create Release and Upload Asset
        uses: softprops/action-gh-release@v1
        with:
          files: ${{ steps.rename_apk.outputs.signedReleaseFile }}
          body_path: history.md
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

#      - name: Gitee Create Release
#        run: |
#          latest_commit=$(git rev-parse HEAD)
#          history=$(cat history.md)
#          curl -v POST https://gitee.com/api/v5/repos/${{ github.repository }}/releases \
#          -H "Content-Type: application/json" \
#          -d '{
#            "access_token": "${{ secrets.GITEE_ACCESS_TOKEN}}",
#            "tag_name": "${{ github.ref_name }}",
#            "name": "Release ${{ github.ref_name }}",
#            "body": "'"$history"'",
#            "prerelease": false,
#            "target_commitish": "'"$latest_commit"'"
#          }'
